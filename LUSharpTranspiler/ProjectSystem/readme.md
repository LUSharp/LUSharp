# Project management

# Todo
- Implement project config as well as runtime config additions


# Project setup

There will be 3 types of class locations
client, server, Replicated(BOTH).

Client scripts will only be ran on the client/localplayer

Server scripts will run on the server.

Replicated scripts will be contained in Replicated Storage.